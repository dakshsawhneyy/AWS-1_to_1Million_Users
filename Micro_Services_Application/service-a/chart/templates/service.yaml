apiVersion: v1
kind: Service
metadata:
  name: {{ include "svc-a.fullname" . }}
  labels: {{- include "svc-a.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "svc-a.selectorLabels" . | nindent 4 }}
