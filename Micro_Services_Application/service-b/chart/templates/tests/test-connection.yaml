apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "svc-b.fullname" . }}-test-connection"
  labels:
    {{- include "svc-b.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "svc-b.fullname" . }}:{{ .Values.service.port }}/actuator/health/liveness']
  restartPolicy: Never
